[package]
name = "bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
manifold = { path = "../manifold" }
hypergraph = { path = "../hypergraph" }
nalgebra = "0.32.3"
