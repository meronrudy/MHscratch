[package]
name = "exec"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
manifold = { path = "../manifold" }
hypergraph = { path = "../hypergraph" }
bridge = { path = "../bridge" }

[dev-dependencies]
